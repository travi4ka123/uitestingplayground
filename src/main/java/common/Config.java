package common;

public class Config {
    public static final String PLATFORM_AND_BROWSER = "win_chrome";
    /**
     * clear cache
     * if True = cache will be cleared
     */
    public static final Boolean CLEAR_CACHE_COOKIES = true;

    /**
     * to close (TRUE) browser after test suite completed
     */
    public static final Boolean CLOSE_BROWSER = true;
}
